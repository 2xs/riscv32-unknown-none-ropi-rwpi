These experiments are provided for illustration only.

Run `make` in this directory to see the available targets.

The main host-side dependencies are:

- `cmake`
- `ninja`
- `qemu-system-riscv32`
- `rg` (`ripgrep`)
